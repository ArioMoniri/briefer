# Briefer — Telegram intake & analysis bot
# Pinned to major versions; setup.sh installs these into a virtualenv.

python-telegram-bot[job-queue]==21.6
anthropic==0.39.0
gspread==6.1.2
google-auth==2.35.0
google-auth-oauthlib==1.2.1
httpx==0.27.2
beautifulsoup4==4.12.3
pypdf==5.0.1
python-dotenv==1.0.1
tenacity==9.0.0
pillow==11.0.0

# Rich media: X/Twitter + video transcription
yt-dlp==2024.12.13
youtube-transcript-api==0.6.2
# Local speech-to-text (CPU, int8). Heavy-ish; downloads a model on first
# use. Disable with ENABLE_TRANSCRIPTION=0 if you don't need video/audio.
faster-whisper==1.0.3
